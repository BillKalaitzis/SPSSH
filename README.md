# Simplified Parallel SSH

Simplified Parallel SSH is a bash wrapper for the parallel-ssh (pssh) program. <br />
The program's intent is to execute in parallel a series of commands in a Linux-powered cluster.
